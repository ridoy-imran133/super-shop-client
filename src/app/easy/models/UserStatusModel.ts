import { FileModel } from "./FileModel";

export class UserStatusModel {
  Id: string;
  UserId: string;
  Name: string;
  // FileName: string;
  // FileLocation: string;
  // FileType: string;
  // FileSize: number;
  // FileByte: any;
  CreatedDate: Date;
  FileModel: FileModel;
}
